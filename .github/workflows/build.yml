name: Build app executable
on:
  push:
    tags:
      - v*
jobs:
  build_executable:
    strategy:
      matrix:
        include:
          - os: windows-latest
            args: "--bundles msi updater"
          - os: ubuntu-latest
            args: "--bundles appimage"
          - os: macos-latest
            args: "--bundles app"
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10
      - name: Install frontend dependencies
        run: pnpm install
      - name: install dependencies (ubuntu only)
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libasound2-dev libpango1.0-dev libgtk-3-dev libglib2.0-dev
      - uses: tauri-apps/tauri-action@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: v__VERSION__
          releaseName: "App v__VERSION__"
          prerelease: false
          args: ${{ matrix.args }}
